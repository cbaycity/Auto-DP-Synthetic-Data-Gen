Dataset Source - Kaggle: https://www.kaggle.com/datasets/rtatman/lego-database?select=sets.csv

Why use this dataset:
The `sets.csv` file in the LEGO Database Kaggle challenge has ~10k rows with a mix of categorical and numerical columns.

# Interesting Question:
Will the `sets.csv` file have `theme_id` be listed as categorical but the `themes.csv` file will list the `id` (or `parent_id`) columns as continuous? This is the most likely outcome but seems 